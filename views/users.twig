<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <main>
    {% for user in users %}
        <p>{{user.nom}}</p>
        <p>{{user.prenom}}</p>
        <p>{{user.age}}</p>
    {% endfor %}
    </main>
</body>
</html>